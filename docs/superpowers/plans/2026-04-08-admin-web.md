# HealthSync Admin Web 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 HealthSync 服务端新增一个带鉴权的后台管理界面，支持用户列表、上传记录分页排序、详情查看和完整 JSON 展示。

**Architecture:** 新增 `public/admin/index.html`（Vue 3 + ElementPlus CDN 单页应用），Express 新增 `admin-web.controller.ts` + `admin-web.routes.ts` 提供 5 个 JSON API，使用独立 HMAC-signed Cookie 鉴权，与现有 JWT 体系完全隔离。

**Tech Stack:** Vue 3 (CDN global build)、Vue Router 4 (hash mode)、ElementPlus CDN、Express、Node.js crypto（内置，无需新增依赖）

---

## 文件映射

| 操作 | 文件 | 职责 |
|------|------|------|
| 新增 | `public/admin/index.html` | Vue SPA Shell：登录/用户列表/上传列表/详情页 |
| 新增 | `src/controllers/admin-web.controller.ts` | login、logout、getUsers、getUserUploads、getUploadDetail |
| 新增 | `src/routes/admin-web.routes.ts` | 挂载 5 个端点 + requireAdminSession 中间件引用 |
| 修改 | `src/routes/index.ts` | 在 `/health` 之前挂载 `/health/web` → adminWebRoutes |
| 修改 | `.env.example` | 添加 ADMIN_WEB_PASSWORD 说明 |

---

### Task 1: 添加 ADMIN_WEB_PASSWORD 到环境配置

**Files:**
- Modify: `server/health-sync-server/.env.example`

- [ ] **Step 1: 在 .env.example 末尾追加配置项**

打开 `server/health-sync-server/.env.example`，在文件末尾添加：

```
# Admin Web Interface
ADMIN_WEB_PASSWORD=change-this-to-a-strong-password
```

- [ ] **Step 2: 在本地 .env 中也添加实际密码**

打开 `server/health-sync-server/.env`（或 `.env.development.local`），添加：

```
ADMIN_WEB_PASSWORD=your-actual-password-here
```

- [ ] **Step 3: 验证服务器能读到该变量**

```bash
cd server/health-sync-server
node -e "require('dotenv').config(); console.log('ADMIN_WEB_PASSWORD:', process.env.ADMIN_WEB_PASSWORD ? '已设置' : '未设置')"
```

预期输出：`ADMIN_WEB_PASSWORD: 已设置`

- [ ] **Step 4: Commit**

```bash
cd server/health-sync-server
git add .env.example
git commit -m "chore: add ADMIN_WEB_PASSWORD to env example"
```

---

### Task 2: 新增 admin-web.controller.ts

**Files:**
- Create: `server/health-sync-server/src/controllers/admin-web.controller.ts`

- [ ] **Step 1: 创建控制器文件**

创建 `server/health-sync-server/src/controllers/admin-web.controller.ts`，内容如下：

```typescript
import { Request, Response, NextFunction } from 'express';
import { createHmac, timingSafeEqual } from 'crypto';
import path from 'path';
import { prisma } from '../models/prisma';
import { createLogger } from '../utils/logger';

const logger = createLogger('AdminWebController');

const COOKIE_NAME = 'admin_session';
const COOKIE_MAX_AGE_MS = 7 * 24 * 60 * 60 * 1000; // 7 days in ms

function signToken(timestamp: number, secret: string): string {
  const hmac = createHmac('sha256', secret).update(String(timestamp)).digest('hex');
  return Buffer.from(`${timestamp}.${hmac}`).toString('base64');
}

function verifyToken(token: string, secret: string): boolean {
  try {
    const decoded = Buffer.from(token, 'base64').toString('utf8');
    const dotIdx = decoded.indexOf('.');
    if (dotIdx < 0) return false;
    const tsStr = decoded.slice(0, dotIdx);
    const hmac = decoded.slice(dotIdx + 1);
    const timestamp = parseInt(tsStr, 10);
    if (isNaN(timestamp) || Date.now() - timestamp > COOKIE_MAX_AGE_MS) return false;
    const expected = createHmac('sha256', secret).update(tsStr).digest('hex');
    const a = Buffer.from(hmac.padEnd(expected.length));
    const b = Buffer.from(expected);
    if (a.length !== b.length) return false;
    return timingSafeEqual(a, b);
  } catch {
    return false;
  }
}

function parseCookies(cookieHeader: string | undefined): Record<string, string> {
  if (!cookieHeader) return {};
  return cookieHeader.split(';').reduce((acc, c) => {
    const idx = c.indexOf('=');
    if (idx > 0) acc[c.slice(0, idx).trim()] = decodeURIComponent(c.slice(idx + 1).trim());
    return acc;
  }, {} as Record<string, string>);
}

export function requireAdminSession(req: Request, res: Response, next: NextFunction): void {
  const cookies = parseCookies(req.headers.cookie);
  const token = cookies[COOKIE_NAME];
  const secret = process.env.ADMIN_WEB_PASSWORD || '';
  if (!secret || !token || !verifyToken(token, secret)) {
    res.status(401).json({ error: 'Unauthorized' });
    return;
  }
  next();
}

export class AdminWebController {
  serveAdmin(req: Request, res: Response): void {
    res.sendFile(path.join(__dirname, '../../public/admin/index.html'));
  }

  async login(req: Request, res: Response): Promise<void> {
    const { password } = req.body as { password?: string };
    const adminPassword = process.env.ADMIN_WEB_PASSWORD;
    if (!adminPassword) {
      res.status(500).json({ error: 'ADMIN_WEB_PASSWORD not configured' });
      return;
    }
    if (!password || password !== adminPassword) {
      res.status(401).json({ error: '密码错误' });
      return;
    }
    const token = signToken(Date.now(), adminPassword);
    res.cookie(COOKIE_NAME, token, {
      httpOnly: true,
      sameSite: 'strict',
      maxAge: COOKIE_MAX_AGE_MS,
      path: '/',
    });
    res.json({ success: true });
  }

  logout(req: Request, res: Response): void {
    res.clearCookie(COOKIE_NAME, { path: '/' });
    res.json({ success: true });
  }

  async getUsers(req: Request, res: Response): Promise<void> {
    try {
      const users = await prisma.user.findMany({
        include: {
          syncStatus: true,
          _count: { select: { uploads: true } },
        },
        orderBy: { createdAt: 'asc' },
      });

      res.json({
        users: users.map(u => ({
          id: u.id,
          username: u.username,
          isActive: u.isActive,
          totalUploads: u._count.uploads,
          lastSyncAt: u.syncStatus[0]?.lastSyncAt ?? null,
        })),
      });
    } catch (error) {
      logger.error('Admin getUsers error', { error });
      res.status(500).json({ error: 'Internal server error' });
    }
  }

  async getUserUploads(req: Request, res: Response): Promise<void> {
    try {
      const { id } = req.params;
      const page = Math.max(1, parseInt(req.query.page as string) || 1);
      const pageSize = 20;
      const allowedSortFields = ['date', 'recordCount', 'fileSize', 'createdAt'];
      const sortBy = allowedSortFields.includes(req.query.sortBy as string)
        ? (req.query.sortBy as string)
        : 'createdAt';
      const sortOrder: 'asc' | 'desc' = req.query.sortOrder === 'asc' ? 'asc' : 'desc';

      const user = await prisma.user.findUnique({ where: { id } });
      if (!user) {
        res.status(404).json({ error: 'User not found' });
        return;
      }

      const [total, data] = await Promise.all([
        prisma.upload.count({ where: { userId: id } }),
        prisma.upload.findMany({
          where: { userId: id },
          orderBy: { [sortBy]: sortOrder },
          skip: (page - 1) * pageSize,
          take: pageSize,
        }),
      ]);

      res.json({ total, page, pageSize, username: user.username, data });
    } catch (error) {
      logger.error('Admin getUserUploads error', { error });
      res.status(500).json({ error: 'Internal server error' });
    }
  }

  async getUploadDetail(req: Request, res: Response): Promise<void> {
    try {
      const { id } = req.params;
      const upload = await prisma.upload.findUnique({
        where: { id },
        include: { user: true },
      });

      if (!upload) {
        res.status(404).json({ error: 'Upload not found' });
        return;
      }

      let fileContent: string | null = null;
      try {
        const { StorageService } = await import('../services/storage.service.js');
        const storage = new StorageService();
        fileContent = await storage.readPlaintextData(upload.user.username, upload.date);
      } catch {
        // file archived or not found — leave null
      }

      res.json({
        id: upload.id,
        userId: upload.userId,
        username: upload.user.username,
        date: upload.date,
        batchIndex: upload.batchIndex,
        batchTotal: upload.batchTotal,
        recordCount: upload.recordCount,
        fileSize: upload.fileSize,
        status: upload.status,
        checksum: upload.checksum,
        errorMessage: upload.errorMessage ?? null,
        createdAt: upload.createdAt,
        fileContent,
      });
    } catch (error) {
      logger.error('Admin getUploadDetail error', { error });
      res.status(500).json({ error: 'Internal server error' });
    }
  }
}

export const adminWebController = new AdminWebController();
```

- [ ] **Step 2: 验证 TypeScript 无报错**

```bash
cd server/health-sync-server
npx tsc --noEmit
```

预期输出：无错误（或仅有已存在的警告）

- [ ] **Step 3: Commit**

```bash
git add src/controllers/admin-web.controller.ts
git commit -m "feat(admin): add AdminWebController with session auth"
```

---

### Task 3: 新增 admin-web.routes.ts 并挂载

**Files:**
- Create: `server/health-sync-server/src/routes/admin-web.routes.ts`
- Modify: `server/health-sync-server/src/routes/index.ts`

- [ ] **Step 1: 创建路由文件**

创建 `server/health-sync-server/src/routes/admin-web.routes.ts`：

```typescript
import { Router } from 'express';
import { adminWebController, requireAdminSession } from '../controllers/admin-web.controller';

const router = Router();

// Serve SPA shell at /health/web/admin
router.get('/admin', (req, res) => adminWebController.serveAdmin(req, res));

// Auth endpoints (no session required)
router.post('/api/login', (req, res) => adminWebController.login(req, res));
router.post('/api/logout', (req, res) => adminWebController.logout(req, res));

// Protected JSON API
router.get('/api/users', requireAdminSession, (req, res) => adminWebController.getUsers(req, res));
router.get('/api/users/:id/uploads', requireAdminSession, (req, res) => adminWebController.getUserUploads(req, res));
router.get('/api/uploads/:id', requireAdminSession, (req, res) => adminWebController.getUploadDetail(req, res));

export { router as adminWebRoutes };
```

- [ ] **Step 2: 修改 src/routes/index.ts，挂载新路由**

将 `server/health-sync-server/src/routes/index.ts` 改为：

```typescript
import { Router } from 'express';
import { authRoutes } from './auth.routes';
import { healthRoutes } from './health.routes';
import { adminWebRoutes } from './admin-web.routes';

const router = Router();

// Mount routes
router.use('/auth', authRoutes);
// Admin web must be mounted before /health to intercept /health/web/* first
router.use('/health/web', adminWebRoutes);
router.use('/health', healthRoutes);

// Health check
router.get('/health', (req, res) => {
  res.json({ status: 'ok', timestamp: new Date().toISOString() });
});

export default router;
```

- [ ] **Step 3: 验证 TypeScript 无报错**

```bash
cd server/health-sync-server
npx tsc --noEmit
```

预期：无错误

- [ ] **Step 4: 验证现有路由未被破坏**

启动服务器并测试旧接口：

```bash
npm run dev &
sleep 3
curl http://localhost:3000/api/health/web/uploads
# 预期：返回原有 HTML 页面（HTTP 200）

curl http://localhost:3000/api/health/healthcheck
# 预期：{"status":"ok",...}

kill %1
```

- [ ] **Step 5: 验证新 API 端点**

```bash
npm run dev &
sleep 3

# 未登录时应返回 401
curl -s http://localhost:3000/api/health/web/api/users
# 预期：{"error":"Unauthorized"}

# 错误密码应返回 401
curl -s -X POST http://localhost:3000/api/health/web/api/login \
  -H "Content-Type: application/json" \
  -d '{"password":"wrong"}'
# 预期：{"error":"密码错误"}

# 正确密码应返回 200 并设置 cookie
curl -s -c /tmp/admin-cookies.txt -X POST http://localhost:3000/api/health/web/api/login \
  -H "Content-Type: application/json" \
  -d '{"password":"YOUR_ACTUAL_PASSWORD"}'
# 预期：{"success":true}

# 携带 cookie 访问用户列表
curl -s -b /tmp/admin-cookies.txt http://localhost:3000/api/health/web/api/users
# 预期：{"users":[...]}

kill %1
```

- [ ] **Step 6: Commit**

```bash
git add src/routes/admin-web.routes.ts src/routes/index.ts
git commit -m "feat(admin): add admin-web routes and mount in router"
```

---

### Task 4: 创建 public/admin/index.html（Vue SPA）

**Files:**
- Create: `server/health-sync-server/public/admin/index.html`

- [ ] **Step 1: 创建目录和 HTML 文件**

```bash
mkdir -p server/health-sync-server/public/admin
```

创建 `server/health-sync-server/public/admin/index.html`，内容如下：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HealthSync Admin</title>
  <link rel="stylesheet" href="https://unpkg.com/element-plus/dist/index.css">
  <style>
    * { box-sizing: border-box; }
    body { margin: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background: #f0f2f5; }
    .nav-bar {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      height: 60px; background: #fff; border-bottom: 1px solid #e8e8e8;
      display: flex; align-items: center; padding: 0 24px;
      justify-content: space-between;
      box-shadow: 0 1px 4px rgba(0,0,0,0.08);
    }
    .nav-title { font-size: 18px; font-weight: 600; color: #409EFF; }
    .page-content { max-width: 1200px; margin: 0 auto; padding: 80px 24px 24px; }
    .stat-row { display: flex; gap: 16px; margin-bottom: 20px; }
    .stat-row .el-card { flex: 1; }
    .login-wrap { display: flex; justify-content: center; align-items: center; min-height: 100vh; }
    .login-card { width: 380px; }
    .login-title { text-align: center; font-size: 22px; font-weight: 600; color: #303133; margin-bottom: 24px; }
    .json-pre {
      background: #1e1e1e; color: #d4d4d4; padding: 16px;
      border-radius: 4px; font-family: 'Courier New', Consolas, monospace;
      font-size: 12px; line-height: 1.6; overflow-y: auto;
      max-height: 600px; white-space: pre; margin: 0;
    }
  </style>
</head>
<body>
<div id="app"></div>

<script src="https://unpkg.com/vue@3/dist/vue.global.prod.js"></script>
<script src="https://unpkg.com/vue-router@4/dist/vue-router.global.prod.js"></script>
<script src="https://unpkg.com/element-plus/dist/index.full.min.js"></script>

<script>
const { createApp, ref, reactive, computed, onMounted } = Vue;
const { createRouter, createWebHashHistory, useRoute, useRouter } = VueRouter;
const { ElMessage } = ElementPlus;

// Derive API base from current URL so it works in both dev and production
// e.g. /healthsync/api/health/web/admin → /healthsync/api/health/web/api
const API_BASE = window.location.pathname.replace(/\/admin\/?$/, '') + '/api';

async function apiFetch(path, options = {}) {
  const res = await fetch(API_BASE + path, {
    credentials: 'same-origin',
    headers: { 'Content-Type': 'application/json', ...(options.headers || {}) },
    ...options,
  });
  if (res.status === 401) {
    router.push('/login');
    throw new Error('Unauthorized');
  }
  return res;
}

// ── Login Page ─────────────────────────────────────────────────────────────
const LoginPage = {
  template: `
    <div class="login-wrap">
      <el-card class="login-card">
        <div class="login-title">🩺 HealthSync Admin</div>
        <el-form @submit.prevent="handleLogin">
          <el-form-item>
            <el-input
              v-model="password"
              type="password"
              placeholder="管理员密码"
              size="large"
              show-password
            />
          </el-form-item>
          <el-form-item>
            <el-button
              type="primary"
              native-type="submit"
              size="large"
              style="width:100%"
              :loading="loading"
            >登录</el-button>
          </el-form-item>
        </el-form>
      </el-card>
    </div>
  `,
  setup() {
    const router = useRouter();
    const password = ref('');
    const loading = ref(false);

    async function handleLogin() {
      if (!password.value.trim()) return;
      loading.value = true;
      try {
        const res = await fetch(API_BASE + '/login', {
          method: 'POST',
          credentials: 'same-origin',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ password: password.value }),
        });
        if (res.ok) {
          router.push('/users');
        } else {
          const data = await res.json();
          ElMessage.error(data.error || '登录失败');
        }
      } catch {
        ElMessage.error('网络错误');
      } finally {
        loading.value = false;
      }
    }

    return { password, loading, handleLogin };
  },
};

// ── App Layout (shared nav bar) ────────────────────────────────────────────
const AppLayout = {
  template: `
    <div>
      <div class="nav-bar">
        <span class="nav-title">🩺 HealthSync Admin</span>
        <el-button size="small" @click="handleLogout">退出</el-button>
      </div>
      <div class="page-content">
        <router-view />
      </div>
    </div>
  `,
  setup() {
    const router = useRouter();
    async function handleLogout() {
      try {
        await fetch(API_BASE + '/logout', { method: 'POST', credentials: 'same-origin' });
      } catch {}
      router.push('/login');
    }
    return { handleLogout };
  },
};

// ── User List Page (/users) ────────────────────────────────────────────────
const UserListPage = {
  template: `
    <div>
      <div class="stat-row">
        <el-card>
          <el-statistic title="用户总数" :value="users.length" />
        </el-card>
        <el-card>
          <el-statistic title="总上传批次" :value="totalUploads" />
        </el-card>
      </div>
      <el-card>
        <el-table
          :data="users"
          v-loading="loading"
          row-key="id"
          style="width:100%;cursor:pointer"
          @row-click="goToUser"
        >
          <el-table-column prop="username" label="用户名" />
          <el-table-column prop="totalUploads" label="总上传批次" />
          <el-table-column label="最后同步时间">
            <template #default="{ row }">
              {{ row.lastSyncAt ? new Date(row.lastSyncAt).toLocaleString('zh-CN') : '从未同步' }}
            </template>
          </el-table-column>
          <el-table-column label="状态" width="90">
            <template #default="{ row }">
              <el-tag :type="row.isActive ? 'success' : 'danger'" size="small">
                {{ row.isActive ? '活跃' : '禁用' }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column label="操作" width="100">
            <template #default="{ row }">
              <el-button size="small" @click.stop="goToUser(row)">查看数据</el-button>
            </template>
          </el-table-column>
        </el-table>
      </el-card>
    </div>
  `,
  setup() {
    const router = useRouter();
    const users = ref([]);
    const loading = ref(false);
    const totalUploads = computed(() =>
      users.value.reduce((s, u) => s + (u.totalUploads || 0), 0)
    );

    onMounted(async () => {
      loading.value = true;
      try {
        const res = await apiFetch('/users');
        const data = await res.json();
        users.value = data.users;
      } catch (e) {
        if (e.message !== 'Unauthorized') ElMessage.error('加载用户列表失败');
      } finally {
        loading.value = false;
      }
    });

    function goToUser(row) {
      router.push('/users/' + row.id);
    }

    return { users, loading, totalUploads, goToUser };
  },
};

// ── User Uploads Page (/users/:id) ─────────────────────────────────────────
const UserUploadsPage = {
  template: `
    <div>
      <el-breadcrumb separator="/" style="margin-bottom:16px">
        <el-breadcrumb-item :to="{ path: '/users' }">用户列表</el-breadcrumb-item>
        <el-breadcrumb-item>{{ username }}</el-breadcrumb-item>
      </el-breadcrumb>
      <el-card>
        <el-table
          :data="uploads"
          v-loading="loading"
          row-key="id"
          style="width:100%"
          @sort-change="handleSortChange"
        >
          <el-table-column prop="date" label="日期" sortable="custom" min-width="110" />
          <el-table-column label="批次" width="80">
            <template #default="{ row }">{{ row.batchIndex + 1 }} / {{ row.batchTotal }}</template>
          </el-table-column>
          <el-table-column prop="recordCount" label="记录数" sortable="custom" width="90" />
          <el-table-column prop="fileSize" label="文件大小" sortable="custom" width="110">
            <template #default="{ row }">{{ (row.fileSize / 1024).toFixed(1) }} KB</template>
          </el-table-column>
          <el-table-column label="状态" width="90">
            <template #default="{ row }">
              <el-tag :type="statusType(row.status)" size="small">{{ statusLabel(row.status) }}</el-tag>
            </template>
          </el-table-column>
          <el-table-column prop="createdAt" label="上传时间" sortable="custom" min-width="160">
            <template #default="{ row }">{{ new Date(row.createdAt).toLocaleString('zh-CN') }}</template>
          </el-table-column>
          <el-table-column label="操作" width="80">
            <template #default="{ row }">
              <el-button size="small" @click="goToUpload(row.id)">详情</el-button>
            </template>
          </el-table-column>
        </el-table>
        <div style="display:flex;justify-content:flex-end;margin-top:16px">
          <el-pagination
            v-model:current-page="page"
            :page-size="20"
            :total="total"
            layout="total, prev, pager, next"
            @current-change="loadData"
          />
        </div>
      </el-card>
    </div>
  `,
  setup() {
    const route = useRoute();
    const router = useRouter();
    const uploads = ref([]);
    const loading = ref(false);
    const total = ref(0);
    const page = ref(1);
    const sortBy = ref('createdAt');
    const sortOrder = ref('desc');
    const username = ref('');

    async function loadData() {
      loading.value = true;
      try {
        const params = new URLSearchParams({
          page: String(page.value),
          pageSize: '20',
          sortBy: sortBy.value,
          sortOrder: sortOrder.value,
        });
        const res = await apiFetch('/users/' + route.params.id + '/uploads?' + params);
        const data = await res.json();
        uploads.value = data.data;
        total.value = data.total;
        username.value = data.username;
      } catch (e) {
        if (e.message !== 'Unauthorized') ElMessage.error('加载上传记录失败');
      } finally {
        loading.value = false;
      }
    }

    function handleSortChange({ prop, order }) {
      if (prop) {
        sortBy.value = prop;
        sortOrder.value = order === 'ascending' ? 'asc' : 'desc';
      }
      page.value = 1;
      loadData();
    }

    function goToUpload(id) { router.push('/uploads/' + id); }

    function statusType(s) {
      return s === 'completed' ? 'success' : s === 'pending' ? 'warning' : 'danger';
    }
    function statusLabel(s) {
      return s === 'completed' ? '完成' : s === 'pending' ? '处理中' : '失败';
    }

    onMounted(loadData);

    return { uploads, loading, total, page, username, loadData, handleSortChange, goToUpload, statusType, statusLabel };
  },
};

// ── Upload Detail Page (/uploads/:id) ─────────────────────────────────────
const UploadDetailPage = {
  template: `
    <div v-if="upload">
      <el-breadcrumb separator="/" style="margin-bottom:16px">
        <el-breadcrumb-item :to="{ path: '/users' }">用户列表</el-breadcrumb-item>
        <el-breadcrumb-item :to="{ path: '/users/' + upload.userId }">{{ upload.username }}</el-breadcrumb-item>
        <el-breadcrumb-item>上传详情</el-breadcrumb-item>
      </el-breadcrumb>

      <el-card style="margin-bottom:16px">
        <el-descriptions title="基本信息" :column="2" border>
          <el-descriptions-item label="记录 ID">
            <span style="font-family:monospace;font-size:11px">{{ upload.id }}</span>
          </el-descriptions-item>
          <el-descriptions-item label="用户">{{ upload.username }}</el-descriptions-item>
          <el-descriptions-item label="日期">{{ upload.date }}</el-descriptions-item>
          <el-descriptions-item label="批次">{{ upload.batchIndex + 1 }} / {{ upload.batchTotal }}</el-descriptions-item>
          <el-descriptions-item label="记录数">{{ upload.recordCount }}</el-descriptions-item>
          <el-descriptions-item label="文件大小">
            {{ upload.fileSize }} 字节（{{ (upload.fileSize / 1024).toFixed(2) }} KB）
          </el-descriptions-item>
          <el-descriptions-item label="状态">
            <el-tag :type="statusType(upload.status)">{{ statusLabel(upload.status) }}</el-tag>
          </el-descriptions-item>
          <el-descriptions-item label="校验和">
            <span style="font-family:monospace;font-size:11px;word-break:break-all">{{ upload.checksum }}</span>
          </el-descriptions-item>
          <el-descriptions-item label="上传时间">
            {{ new Date(upload.createdAt).toLocaleString('zh-CN') }}
          </el-descriptions-item>
          <el-descriptions-item v-if="upload.errorMessage" label="错误信息">
            <span style="color:#f56c6c">{{ upload.errorMessage }}</span>
          </el-descriptions-item>
        </el-descriptions>
      </el-card>

      <el-card>
        <template #header>
          <div style="display:flex;justify-content:space-between;align-items:center">
            <span>健康数据 JSON</span>
            <el-button v-if="upload.fileContent" size="small" @click="jsonExpanded = !jsonExpanded">
              {{ jsonExpanded ? '收起' : '展开' }}
            </el-button>
          </div>
        </template>
        <div v-if="!upload.fileContent" style="color:#909399;padding:20px;text-align:center">
          文件已归档或不存在
        </div>
        <div v-else-if="!jsonExpanded" style="color:#909399;font-size:13px;padding:4px 0">
          点击「展开」查看完整 JSON 数据（{{ upload.fileSize }} 字节）
        </div>
        <pre v-else class="json-pre">{{ formattedJson }}</pre>
      </el-card>
    </div>
    <div v-else-if="loading" style="padding:40px;text-align:center">
      <el-text type="info">加载中...</el-text>
    </div>
    <el-empty v-else description="记录不存在" />
  `,
  setup() {
    const route = useRoute();
    const upload = ref(null);
    const loading = ref(false);
    const jsonExpanded = ref(false);

    const formattedJson = computed(() => {
      if (!upload.value?.fileContent) return '';
      try {
        return JSON.stringify(JSON.parse(upload.value.fileContent), null, 2);
      } catch {
        return upload.value.fileContent;
      }
    });

    function statusType(s) {
      return s === 'completed' ? 'success' : s === 'pending' ? 'warning' : 'danger';
    }
    function statusLabel(s) {
      return s === 'completed' ? '完成' : s === 'pending' ? '处理中' : '失败';
    }

    onMounted(async () => {
      loading.value = true;
      try {
        const res = await apiFetch('/uploads/' + route.params.id);
        upload.value = await res.json();
      } catch (e) {
        if (e.message !== 'Unauthorized') ElMessage.error('加载详情失败');
      } finally {
        loading.value = false;
      }
    });

    return { upload, loading, jsonExpanded, formattedJson, statusType, statusLabel };
  },
};

// ── Router ─────────────────────────────────────────────────────────────────
const routes = [
  { path: '/', redirect: '/users' },
  { path: '/login', component: LoginPage },
  {
    path: '/',
    component: AppLayout,
    children: [
      { path: 'users',          component: UserListPage },
      { path: 'users/:id',      component: UserUploadsPage },
      { path: 'uploads/:id',    component: UploadDetailPage },
    ],
  },
];

const router = createRouter({
  history: createWebHashHistory(),
  routes,
});

// Redirect unauthenticated users: handled by apiFetch returning 401 → push('/login')

// ── Mount ──────────────────────────────────────────────────────────────────
const app = createApp({ template: '<router-view />' });
app.use(router);
app.use(ElementPlus);
app.mount('#app');
</script>
</body>
</html>
```

- [ ] **Step 2: 验证 SPA 页面可访问**

```bash
cd server/health-sync-server
npm run dev &
sleep 3

# 应该返回 HTTP 200 和 HTML
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/health/web/admin
# 预期：200

kill %1
```

- [ ] **Step 3: 浏览器中完整验证流程**

启动服务 `npm run dev`，打开浏览器访问：

```
http://localhost:3000/api/health/web/admin
```

按顺序验证：
1. 自动跳转到 `/#/login`，显示登录卡片
2. 输入错误密码 → 显示红色错误提示
3. 输入正确密码 → 跳转到 `/#/users`，显示用户列表和统计卡片
4. 点击用户行 → 跳转到 `/#/users/:id`，显示上传记录表格
5. 点击表头「日期」→ 排序变化，数据重新加载
6. 翻页测试（如有足够数据）
7. 点击某条记录「详情」→ 跳转到 `/#/uploads/:id`，显示 `ElDescriptions`
8. 点击「展开」→ 显示格式化 JSON（暗色背景）
9. 点击「收起」→ JSON 隐藏
10. 点击面包屑 → 正确导航回上级页面
11. 点击「退出」→ 跳回登录页，再次访问 `/api/users` 返回 401

- [ ] **Step 4: 验证旧页面依然正常**

```bash
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/api/health/web/uploads
# 预期：200（旧页面未被破坏）
```

- [ ] **Step 5: Commit**

```bash
git add public/admin/index.html
git commit -m "feat(admin): add Vue SPA admin interface"
```

---

### Task 5: 部署到服务器

**Files:** 仅操作服务器

- [ ] **Step 1: 在服务器 .env 中添加 ADMIN_WEB_PASSWORD**

SSH 登录服务器，编辑 `/path/to/health-sync-server/.env`，添加：

```
ADMIN_WEB_PASSWORD=your-strong-password-here
```

- [ ] **Step 2: 拉取代码并重启**

```bash
git pull origin main
# 如果是 Docker 部署：
docker compose restart health-sync-server
# 或直接：
pm2 restart health-sync-server
```

- [ ] **Step 3: 验证生产环境**

```bash
curl -s -o /dev/null -w "%{http_code}" https://markmager.cc/healthsync/api/health/web/admin
# 预期：200
```

浏览器访问 `https://markmager.cc/healthsync/api/health/web/admin`，完整重跑 Task 4 Step 3 的验证流程。

- [ ] **Step 4: Commit（如有服务器端配置文件变更）**

服务器的 `.env` 不提交到 git（gitignore 中已排除），无需 commit。
